/** Capitalized per this phase's spec, matching Report/Insight's convention from Phase 6. */
const CONVERSATION_STATUS = Object.freeze({
  OPEN: 'Open',
  CLOSED: 'Closed',
  ARCHIVED: 'Archived',
});

module.exports = { CONVERSATION_STATUS };
